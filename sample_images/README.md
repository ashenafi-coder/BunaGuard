# Sample Images

These three coffee-leaf images are included so the Gradio demo can be
tested quickly:

```text
healthy_example.jpg
leaf_rust_example.jpg
phoma_example.jpg
```

They are copies from the prepared dataset and are included only for a
quick demonstration. They must not be treated as a separate evaluation
set.
